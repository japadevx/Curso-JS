let produtos = [{ nome: 'Camiseta', preco: 20 }, { nome: 'Celular', preco: 500 }];
let quantidades = [3, 1];
function total((produtos, quantidades){
    return produtos.map((produtos,index)=>{
        let venda =
    })
})